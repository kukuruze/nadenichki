@model IEnumerable<ConfigurationWebShopDemo.Models.CPUs>
@{
    ViewData["Title"] = "CpuView";
}

<div class="container p-3">
    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead style="color: white; text-align:center">
                <tr>
                    <th>
                        Model CPU
                    </th>
                    <th>
                        CPU Price
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
<<<<<<< Updated upstream
                    <tr>
                        <td width="50%" style="color: white">@obj.cpu_model</td>
                        <td width="50%" style="color: white">@obj.cpu_price</td>
                        
=======
                <tr>
                    <td width="50%" style="color: white">@obj.cpu_model</td>
                    <td width="50%" style="color: white">@obj.cpu_price</td>
                    @if (Logged.LoggedIn == true)
                    {
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Cart" asp-route-Id="@obj.cpu_id" asp-action="AddToCartCPU" class="btn btn-primary mx-2">
                                    <i class="fas fa-edit"></i>
                                    Add To Cart
                                </a>
                                </div>
                            </td>
                     }
>>>>>>> Stashed changes
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p style="color: white">Изчерпани са количествата</p>
    }
</div>
